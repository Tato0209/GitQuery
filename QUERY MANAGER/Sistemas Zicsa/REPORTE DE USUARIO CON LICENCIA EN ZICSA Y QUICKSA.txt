CALL CL_REPORTE_USUARIOS_LICENCIA

/*CREATE PROCEDURE CL_REPORTE_USUARIOS_LICENCIA
AS
BEGIN
SELECT DISTINCT  * FROM (
SELECT T0."USER_CODE",T0."U_NAME",T1."Name", t0.USERID,
CASE WHEN T0."MobileUser"='N' THEN 'NO' ELSE 'SI' END "Usuario Movil",
CASE WHEN T0."SUPERUSER"='N' THEN 'NO' ELSE 'SI' END "Super Usuario",
CASE WHEN T0."Locked"='N' THEN 'NO' ELSE 'SI' END "Bloqueado",
"U_CL_PROPIETARIO" "EMPRESA"
FROM "SBO_ZICSA_PRODUCCION"."OUSR" T0 
LEFT JOIN  "SBO_ZICSA_PRODUCCION"."OUDP" T1 ON T0."Department" = T1."Code"
WHERE T0."GROUPS"<>99 AND T0."Locked"='N'
UNION ALL 

SELECT T0."USER_CODE",T0."U_NAME",T1."Name", t0.USERID,
CASE WHEN T0."MobileUser"='N' THEN 'NO' ELSE 'SI' END "Usuario Movil",
CASE WHEN T0."SUPERUSER"='N' THEN 'NO' ELSE 'SI' END "Super Usuario",
CASE WHEN T0."Locked"='N' THEN 'NO' ELSE 'SI' END "Bloqueado",
"U_CL_PROPIETARIO" "EMPRESA"
FROM  "SBO_QUICKSA_PRODUCCION"."OUSR" T0 
LEFT JOIN  "SBO_QUICKSA_PRODUCCION"."OUDP" T1 ON T0."Department" = T1."Code"
WHERE T0."GROUPS"<>99 AND T0."Locked"='N') 
ORDER BY USERID ASC;
END*/