IF $[$38.43.NUMBER]='540000006'
THEN
SELECT T1."BaseLine" FROM OPQT T0 INNER JOIN PQT1 T1 ON  T0."DocEntry"=T1."DocEntry" WHERE T0."DocNum"=$[$38.44.NUMBER] and T1."ItemCode"=$[$38.1.1];
ELSE
SELECT $[$38.46.NUMBER] FROM DUMMY;
END IF;