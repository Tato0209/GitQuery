IF $[$38.43.NUMBER]='540000006'
THEN
SELECT T1."BaseEntry" FROM OPQT T0 INNER JOIN PQT1 T1 ON  T0."DocEntry"=T1."DocEntry" WHERE T0."DocNum"=$[$38.44.NUMBER];
ELSE
SELECT $[$38.45.NUMBER] FROM DUMMY;
END IF;